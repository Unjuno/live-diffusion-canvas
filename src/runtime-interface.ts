export type DiffusionIntervention = { requestId:number; sessionId:string; prompt:string; guideComposite?:string; importedImage?:string; guideEraseMask?:string; guideInfluence:number; globalExplorationNoiseStrength:number; temperature?:number; noiseBrushActive:boolean; activeNoiseMask?:string; localRejectionStrength:number; updatesToAdvance:number; phase:'explore'|'finish' };
export type DiffusionRuntimeResponse = { requestId:number; sessionId:string; previewImage:string; seed:number; latencyMs:number };
export interface DiffusionRuntime { createSession(seed:number):Promise<string>; applyIntervention(input:DiffusionIntervention):Promise<DiffusionRuntimeResponse>; finish(input:DiffusionIntervention):Promise<DiffusionRuntimeResponse>; }
